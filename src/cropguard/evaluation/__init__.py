"""Model evaluation: statistical comparison, calibration, and error analysis.

Import-light on purpose — `pip install ".[eval]"` pulls only numpy/scipy/scikit-learn, so
hypothesis tests can run on saved predictions without a torch environment.
"""
